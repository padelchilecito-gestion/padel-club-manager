{
  "name": "padel-club-manager-server",
  "version": "1.1.0",
  "description": "Backend for Padel Club Manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon --watch . --ignore ../frontend/ server.js",
    "db:seed": "node seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.0", // Ensure this version is okay, previous files showed 1.41.3
    // --- ADDED ---
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "date-fns-tz": "^3.2.0", // Ensure this version is okay, previous files showed 3.1.3
    "dotenv": "^16.3.1", // Ensure this version is okay, previous files showed 16.4.5
    "express": "^4.18.2", // Ensure this version is okay, previous files showed 4.19.2
    // --- ADDED ---
    "express-async-handler": "^1.2.0", // Might be needed by middlewares
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^7.3.1",
    // --- ADDED ---
    "helmet": "^7.1.0", 
    "hpp": "^0.2.3",
    "jsonwebtoken": "^9.0.2",
    "mercadopago": "^2.0.8", // Ensure this version is okay, previous files showed 2.0.11
    "mongoose": "^7.6.3", // Ensure this version is okay, previous files showed 8.4.1
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "multer-storage-cloudinary": "^4.0.0",
    // --- ADDED (Optional but good practice) ---
    // "node-cron": "^3.0.3", // For scheduled tasks if you uncomment them later
    "socket.io": "^4.7.2", // Ensure this version is okay, previous files showed 4.7.5
    // "twilio": "^5.2.0", // For notifications if used
    "xss-clean": "^0.1.4"
    // --- END ADDED ---
  },
  "devDependencies": {
    "nodemon": "^3.0.1" // Ensure this version is okay, previous files showed 3.1.2
  }
}

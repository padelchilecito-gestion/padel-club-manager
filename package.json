{
  "name": "padel-club-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix server\" \"npm run dev --prefix frontend\"",
    "install": "npm install --prefix server && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}